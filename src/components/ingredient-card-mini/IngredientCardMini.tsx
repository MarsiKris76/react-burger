import {IngredientCardMiniProps} from "../../types/types";

export const IngredientCardMini = ({ingredient, count = 0, onClick}: IngredientCardMiniProps) => {
    return (
        <div>
            byuhblbtyn
        </div>
    );
};